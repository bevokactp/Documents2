const print = console.log.bind(console);
const log = function() { return console.log.apply(console, arguments); };


"use strict";
let, const, var
iner local global
1 ? = : 0
{property_name1: varname1, property_name2: varname2, ...other} = {} varname1,,varname3 = []
"str ${str_varname}"
String.raw
`m-line`
teplated_tag`${str_varname}.`;
for await of
if else switch
Object.create({})
Symbol
(varname, ...) [...array]
new class super constructor this extends static get set instanceof
new () => {} this
void function
.next() () => { yield }
Array.prototype.map
try catch finally throw
async await
goto
import export default Modules
Promise.then()
Proxy Reflect
debugger

